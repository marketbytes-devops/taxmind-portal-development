---
trigger: always_on
---

Hi, this is a new project called “TaxMind.” The project was originally developed by another company, and the development cost was very high. Due to this, the client has now assigned the project to our company. The client has informed us that some additional implementations and rework are required in the existing codebase. We have accepted these requirements. Our priority is to implement the requested changes without affecting the existing code functionality or the already hosted system. Most importantly, we will ensure that no existing data is lost, as it is highly critical. I have no base in Node.js and Vue.js also. So help me to guide also.

We can implement it one by one.